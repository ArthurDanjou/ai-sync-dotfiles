{
  "name": "dotfiles",
  "displayName": "Dotfiles",
  "type": "module",
  "version": "0.2.0",
  "private": true,
  "packageManager": "bun@1.3.14",
  "description": "Mac dotfiles with MCP server exporter for Zed, OpenCode, Claude Desktop, Claude Code, Codex and LM Studio",
  "license": "MIT",
  "scripts": {
    "build": "tsx scripts/mcp/index.ts",
    "setup": "tsx scripts/setup/dotfiles.ts",
    "install:all": "tsx scripts/setup/install.ts",
    "install:zed": "tsx scripts/setup/install.ts zed",
    "install:claude": "tsx scripts/setup/install.ts claude",
    "install:claude-code": "tsx scripts/setup/install.ts claudeCode",
    "install:opencode": "tsx scripts/setup/install.ts opencode",
    "install:codex": "tsx scripts/setup/install.ts codex",
    "install:lmstudio": "tsx scripts/setup/install.ts lmstudio",
    "audit": "tsx scripts/setup/audit.ts",
    "typecheck": "tsc --noEmit",
    "check": "bun run build && bun run typecheck && bun run audit"
  },
  "devDependencies": {
    "@types/node": "26.1.2",
    "tsx": "4.23.1",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
